# 更新日志

本文件记录 AstrBot 群典插件可确认的版本变更。格式参考 Keep a Changelog，版本号遵循项目元数据。

## [1.1.2] - 2026-08-19

### 新增

- 合并转发支持可配置深度的递归本地化，保存嵌套节点、原始转发 ID、回复、时间和媒体。
- 回复读取兼容内嵌快照、`get_msg`、CQ 字符串、嵌套响应和群历史记录回退。
- 新增 `/群典 @某人 <关键词>` 查询，按 QQ ID 限定作者后匹配其本人发送的文字，支持长度上限与群级覆盖。
- Plugin Pages 支持删除合并转发中任意层级的单个节点，并记录节点路径和删除数量审计。

### 修复

- 修复回复消息无法读取、回复在聚合消息中使用文字边框模拟的问题；保存真实消息序号，发送时改为原生回复优先、结构化节点降级。
- 修复爆典无法递归匹配嵌套发送者，以及单个媒体缺失导致整条合并转发被跳过的问题。
- 修复合并转发节点优先显示 QQ 昵称而不是当前群名片的问题。
- 修复 `@Bot 爆典 @某人` 被插件处理后仍继续触发 AstrBot 对话的问题。

### 调整

- 数据格式升级为 Schema v3；继续兼容 v1、v2 数据和备份，并在下一次修改时惰性升级。
- 控制台日志改为每次操作最多一条简短结果；重试过程仅写入 DEBUG，最终发送失败保留 ERROR 堆栈。

## [1.1.1] - 2026-08-19

### 新增

- 插件指令和关键词触发均输出带短追踪 ID 的 INFO 开始与结果日志，不记录金句正文或图片地址。
- 爆典新增独立文字节点、实验性原生时间和隐藏时间三种记录时间显示方式。
- 新增可选的 QQ 头像本地缓存，按 QQ 号复用 160×160 头像，支持有效期、容量淘汰、空白头像降级和后台清理。

### 调整

- 爆典记录正文不再嵌入记录时间或横线分隔符。
- 原始聊天记录改为直接嵌套子节点；平台拒绝时继续降级为扁平合集并记录日志。
- 媒体容量统计包含头像缓存，但头像清理不会删除任何群典记录或群典图片。

## [1.1.0] - 2026-08-19

### 新增

- 新增 `/爆典 @某人 [页码]` 指令和可配置的爆典关键词，按收录时间发送指定成员参与的全部健康记录。
- 新增爆典每页条数、独立权限和群级覆盖；记录超过单页上限时先提示总页数。
- 新增 QQ 内置表情持久化与原生重放。
- 新增商城表情本地化，保存本地图片和必要元数据，原生发送失败时降级为图片。
- 新增递归回复快照，保存被回复消息的作者、文字、图片和表情，并支持深度限制与循环保护。
- Plugin Pages 新增爆典配置，支持查看表情和完整回复关系，并可搜索回复文字。

### 调整

- 数据格式升级为 Schema v2；继续兼容 v1 数据和备份，并在下一次修改时创建备份后惰性升级。
- 回复内容参与哈希去重和删除搜索；相同正文回复不同内容时会视为不同记录。
- 爆典通过 QQ ID 匹配成员，包含目标成员的多作者合并转发整体计为一条记录。
- 含 QQ 表情、商城表情或回复快照的记录绕过 CSS 卡片，改用原生消息链发送。
- 爆典中的合并转发优先嵌套发送；不兼容时降级为扁平合集，并在必要时将商城表情降级为本地图片。

## [1.0.0] - 2026-08-17

初版
